name: pawkar_app
description: "Aplicación oficial de PAWKAR RAYMI PEGUCHE 2026 - Evento deportivo y cultural"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # UI Components
  cupertino_icons: ^1.0.8
  flutter_svg: ^2.0.10+1
  carousel_slider: ^4.2.1
  cached_network_image: ^3.3.1
  shimmer: ^2.0.0
  intl: ^0.20.2
  google_fonts: ^6.1.0
  flutter_screenutil: ^5.9.3
  flutter_staggered_grid_view: ^0.7.0
  pull_to_refresh: ^2.0.0
  flutter_html: ^3.0.0
  flutter_animate: ^4.2.0
  
  # State Management
  provider: ^6.1.2
  
  # Network
  dio: ^5.4.3
  retrofit: ^4.1.1+1
  logger: ^2.0.2
  
  # Local Storage
  shared_preferences: ^2.2.2
  
  # Utils
  url_launcher: ^6.2.2
  share_plus: ^7.2.1
  flutter_dotenv: ^5.1.0
  intl_phone_field: ^3.1.0
  flutter_switch: ^0.3.2
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1
  mockito: ^5.4.4
  build_runner: ^2.4.8
  retrofit_generator: ^10.0.6
  json_serializable: ^6.8.0
  flutter_launcher_icons: ^0.13.1

flutter:
  uses-material-design: true
  
  assets:
    - assets/images/
    - assets/icons/
    - .env

  # Fonts are now loaded using google_fonts package
